<Window x:Class="BookshopWpf.Settings.SettingsWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BookshopWpf.Settings"
             mc:Ignorable="d" 
             Title="BookshopWpf settings"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="60"/> <!--Buttons row-->
        </Grid.RowDefinitions>
        <Grid Grid.Row="1" HorizontalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button x:Name="ButtonOk" Content="Ok" Click="ButtonOk_Click"
                    Grid.Column="0" HorizontalContentAlignment="Center"
                    Width="80" Height="30" Margin="20"/>
            <Button x:Name="ButtonCancel" Content="Cancel" Click="ButtonCancel_Click"
                    Grid.Column="1" HorizontalContentAlignment="Center"
                    Width="80" Height="30"/>
        </Grid>
    </Grid>
</Window>
